#include <stdio.h> //Biblioteca Padrão de Entrada e Saída - Standard Input Output . Header

int main() // Função principal, por onde o código inicia a execução
{
    int idade; // Declaração de varipavel do tipo inteiro
    idade = 27; //Atribuição de valo na variável

    printf("%d", idade); //Printf para escrever o valor da variável na tela

    return 0;
}